//[kraken-api](../../../index.md)/[com.kraken.api.service.camera](../index.md)/[CameraService](index.md)/[turnTo](turn-to.md)

# turnTo

[Kraken API]\
open fun [turnTo](turn-to.md)(actor: Actor)

Rotates the camera to face the specified actor with a default maximum angle of 40 degrees.

#### Parameters

Kraken API

| | |
|---|---|
| actor | the actor to turn towards |

[Kraken API]\
open fun [turnTo](turn-to.md)(actor: Actor, maxAngle: [Int](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin-stdlib/kotlin/-int/index.html))

Rotates the camera to face the specified actor with a custom maximum angle.

#### Parameters

Kraken API

| | |
|---|---|
| actor | the actor to turn towards |
| maxAngle | the maximum angle to rotate in a single adjustment |

[Kraken API]\
open fun [turnTo](turn-to.md)(tileObject: TileObject)

Rotates the camera to face the specified tile object with a default maximum angle of 40 degrees.

#### Parameters

Kraken API

| | |
|---|---|
| tileObject | the tile object to turn towards |

[Kraken API]\
open fun [turnTo](turn-to.md)(tileObject: TileObject, maxAngle: [Int](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin-stdlib/kotlin/-int/index.html))

Rotates the camera to face the specified tile object with a custom maximum angle.

#### Parameters

Kraken API

| | |
|---|---|
| tileObject | the tile object to turn towards |
| maxAngle | the maximum angle to rotate in a single adjustment |

[Kraken API]\
open fun [turnTo](turn-to.md)(localPoint: LocalPoint)

Rotates the camera to face the specified local point with a default maximum angle of 40 degrees.

#### Parameters

Kraken API

| | |
|---|---|
| localPoint | the local point to turn towards |

[Kraken API]\
open fun [turnTo](turn-to.md)(localPoint: LocalPoint, maxAngle: [Int](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin-stdlib/kotlin/-int/index.html))

Rotates the camera to face the specified local point with a custom maximum angle.

#### Parameters

Kraken API

| | |
|---|---|
| localPoint | the local point to turn towards |
| maxAngle | the maximum angle to rotate in a single adjustment |
